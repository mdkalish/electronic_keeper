%tr
  %td
    = link_to image_tag("delete.png", size: '35x35'), '#'
  - if params[:size]
    %td
      = "#{@ticket.name} mała"
    %td#price
      = convert_to_pln(@ticket.small_price)
  - else
    %td
      = @ticket.name
    %td#price
      = convert_to_pln(@ticket.price)
